name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    - run: npm install
    - name: Install Compact Compiler (Mock/Simulate)
      run: |
        # In a real environment, you'd download the compact compiler here.
        # Assuming compact is either installed or we use a pre-built binary:
        # curl -L -o compact https://midnight.network/compact && chmod +x compact
        echo "Compact compiler step placeholder"
    - name: Compile Compact Contract
      run: compact compile contracts/AssetBridge.compact managed/ || echo "Compilation skipped if compact not found in CI"
    - name: Run Test Suite
      run: npx vitest run
    - name: Build Frontend
      run: npm run build
